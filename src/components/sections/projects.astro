---
import { PROJECT } from "../../data/projects";
import ProjectList from "../ListProj.astro";
---

<ul class="group/list space-y-12">
    {PROJECT.map((project) => <ProjectList {...project} />)}
</ul>